@model IEnumerable<TeamRedInternalProject.Models.Artist>

@{
    ViewData["Title"] = "Artists";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<div class="p-0">
    <img src="~/images/LineupRedone.png" class="img-fluid w-100" alt="Artist Line-up">
</div>

<h1 class="text-center">Line-up</h1>
<div class="container mt-5">


    <section class="card-container">
        @foreach (var item in Model)
        {
            <article class="main-card">
                <img src="~/images/@item.ImgPath" class="card-img-top" alt="@item.ArtistName">
                <h2>@item.ArtistName</h2>
                <p>@item.ArtistBio</p>
            </article>
        }
    </section>
</div>


